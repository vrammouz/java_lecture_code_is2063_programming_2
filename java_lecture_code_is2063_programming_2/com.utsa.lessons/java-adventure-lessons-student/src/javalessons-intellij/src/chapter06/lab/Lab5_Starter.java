package chapter06.lab;

/**
 * LAB 5 STARTER  -  Gaddis Ch. 6
 *
 * You are given a finished Item class. Do not modify it.
 *
 * YOUR TASK
 * ---------
 * Write a class called Player, in its own file Player.java, with:
 * Create it inside the chapter06.lab package so IntelliJ adds
 *     package chapter06.lab;
 * as its first line.
 *
 *   1. Private fields for a name (String) and health (int).
 *   2. A constructor taking a name, setting health to 100.
 *   3. An accessor for each field.
 *   4. A method  takeDamage(int points)  that reduces health.
 *   5. A method  isAlive()  that returns true when health is above zero.
 *   6. A method  describe()  that prints the player's name and health.
 *
 * Then in this file, in main:
 *   - Create a Player.
 *   - Create two Item objects.
 *   - Print the player's status, damage them twice, print it again.
 *   - Print the total weight of the two items.
 *
 * DONE WHEN: it compiles and runs, Player has all six members above,
 * and every field in Player is private.
 *
 * WATCH OUT: you cannot reach a private field from outside its class.
 * If you try, read the compiler error carefully -- it tells you exactly
 * what is wrong. That error is your friend.
 */
public class Lab5_Starter
{
   public static void main(String[] args)
   {
      // TODO: create a Player and two Items, then exercise them

   }
}
